
List files committed for a revision (+ commit message):
	svn log --verbose -r REV

Revert Commits:
	svn merge -r UPREV:LOWREV . undo range
	svn merge -c -REV . undo single revision

See a file as it was in an older revision:

	svn cat -r REV file

